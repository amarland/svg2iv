name: svg2iv
description: A command-line application to convert SVG files to Jetpack Compose `ImageVector`s.
# version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.1
  async: ^2.11.0
  cbor: ^6.0.0
  io: ^1.0.4
  svg2iv_common:
    path: ../common

dev_dependencies:
  lints: ^2.1.1
  # test: ^1.24.3
